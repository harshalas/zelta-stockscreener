import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zelta.stockscreener',
  appName: 'Zelta Stock Screener',
  webDir: 'out'
};

export default config;
